import { Head, Link, useForm } from '@inertiajs/react';

import AdminCard from '@/Components/Admin/AdminCard';
import AdminPageHeader from '@/Components/Admin/AdminPageHeader';
import AdminLayout from '@/Layouts/AdminLayout';

function readableLabel(value) { return String(value ?? '-').replaceAll('_', ' ').replaceAll('-', ' ').replace(/\b\w/g, (char) => char.toUpperCase()); }
function formatCurrency(value) { return new Intl.NumberFormat('id-ID', { currency: 'IDR', maximumFractionDigits: 0, style: 'currency' }).format(Number(value ?? 0)); }
function formatDateTimeInput(value = new Date()) { return new Date(value).toISOString().slice(0, 16); }
function FieldError({ message }) { return message ? <p className="mt-1 font-body-sm text-xs text-red-700">{message}</p> : null; }
function TextField({ error, label, name, onChange, type = 'text', value }) { return <label className="block"><span className="font-label-sm text-xs font-bold uppercase tracking-[0.12em] text-gray-500">{label}</span><input className="mt-2 block w-full rounded-2xl border-[#E5E7EB] font-body-sm text-sm text-[#333333] shadow-sm focus:border-[#1E4D3A] focus:ring-[#1E4D3A]" name={name} onChange={onChange} type={type} value={value ?? ''} /><FieldError message={error} /></label>; }
function SelectField({ children, error, label, name, onChange, value }) { return <label className="block"><span className="font-label-sm text-xs font-bold uppercase tracking-[0.12em] text-gray-500">{label}</span><select className="mt-2 block w-full rounded-2xl border-[#E5E7EB] font-body-sm text-sm text-[#333333] shadow-sm focus:border-[#1E4D3A] focus:ring-[#1E4D3A]" name={name} onChange={onChange} value={value ?? ''}>{children}</select><FieldError message={error} /></label>; }
function TextAreaField({ error, label, name, onChange, value }) { return <label className="block"><span className="font-label-sm text-xs font-bold uppercase tracking-[0.12em] text-gray-500">{label}</span><textarea className="mt-2 block w-full rounded-2xl border-[#E5E7EB] font-body-sm text-sm text-[#333333] shadow-sm focus:border-[#1E4D3A] focus:ring-[#1E4D3A]" name={name} onChange={onChange} rows="4" value={value ?? ''} /><FieldError message={error} /></label>; }

function AdminOfflineSalesTambah({ products = [], customerProfiles = [], leads = [], fieldStaff = [], events = [], sources = [] }) {
    const form = useForm({ customer_profile_id: '', lead_id: '', field_staff_id: '', event_id: '', source: 'offline', customer_name: '', customer_whatsapp_number: '', notes: '', sold_at: formatDateTimeInput(), items: [{ row_id: crypto.randomUUID(), product_id: '', quantity: 1 }] });
    const estimatedTotal = form.data.items.reduce((total, item) => {
        const product = products.find((entry) => String(entry.id) === String(item.product_id));
        return total + Number(product?.price ?? 0) * Number(item.quantity ?? 0);
    }, 0);

    function updateItem(index, key, value) {
        form.setData('items', form.data.items.map((item, itemIndex) => itemIndex === index ? { ...item, [key]: value } : item));
    }

    function addItem() { form.setData('items', [...form.data.items, { row_id: crypto.randomUUID(), product_id: '', quantity: 1 }]); }
    function removeItem(index) { form.setData('items', form.data.items.filter((_, itemIndex) => itemIndex !== index)); }
    function submit(event) { event.preventDefault(); form.post(route('admin.offline-sales.store')); }

    return <><Head title="Tambah Offline Sale" /><div className="space-y-8"><AdminPageHeader action={<Link className="rounded-full border border-[#1E4D3A] px-4 py-2 font-body-sm text-sm font-bold text-[#1E4D3A] transition hover:bg-[#1E4D3A] hover:text-white" href={route('admin.offline-sales.index')}>Kembali</Link>} description="Catat penjualan offline. Harga dan total final dihitung ulang oleh server dari data produk." eyebrow="Commerce / Penjualan Offline" title="Tambah Offline Sale" /><form className="space-y-5" onSubmit={submit}><AdminCard className="p-5"><div className="grid grid-cols-1 gap-4 xl:grid-cols-2"><SelectField error={form.errors.source} label="Sumber" name="source" onChange={(event) => form.setData('source', event.target.value)} value={form.data.source}>{sources.map((source) => <option key={source} value={source}>{readableLabel(source)}</option>)}</SelectField><TextField error={form.errors.sold_at} label="Tanggal Terjual" name="sold_at" onChange={(event) => form.setData('sold_at', event.target.value)} type="datetime-local" value={form.data.sold_at} /><TextField error={form.errors.customer_name} label="Nama Customer" name="customer_name" onChange={(event) => form.setData('customer_name', event.target.value)} value={form.data.customer_name} /><TextField error={form.errors.customer_whatsapp_number} label="Customer WhatsApp" name="customer_whatsapp_number" onChange={(event) => form.setData('customer_whatsapp_number', event.target.value)} value={form.data.customer_whatsapp_number} /><SelectField error={form.errors.customer_profile_id} label="Profil Customer" name="customer_profile_id" onChange={(event) => form.setData('customer_profile_id', event.target.value)} value={form.data.customer_profile_id}><option value="">Tidak terhubung</option>{customerProfiles.map((profile) => <option key={profile.id} value={profile.id}>{profile.name}</option>)}</SelectField><SelectField error={form.errors.lead_id} label="Lead" name="lead_id" onChange={(event) => form.setData('lead_id', event.target.value)} value={form.data.lead_id}><option value="">Tidak dari lead</option>{leads.map((lead) => <option key={lead.id} value={lead.id}>{lead.name}</option>)}</SelectField><SelectField error={form.errors.field_staff_id} label="Staff Lapangan" name="field_staff_id" onChange={(event) => form.setData('field_staff_id', event.target.value)} value={form.data.field_staff_id}><option value="">Tidak assigned</option>{fieldStaff.map((user) => <option key={user.id} value={user.id}>{user.name}</option>)}</SelectField><SelectField error={form.errors.event_id} label="Event" name="event_id" onChange={(event) => form.setData('event_id', event.target.value)} value={form.data.event_id}><option value="">Tidak dari event</option>{events.map((event) => <option key={event.id} value={event.id}>{event.name}</option>)}</SelectField><div className="xl:col-span-2"><TextAreaField error={form.errors.notes} label="Catatan" name="notes" onChange={(event) => form.setData('notes', event.target.value)} value={form.data.notes} /></div></div></AdminCard><AdminCard className="p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-label-sm text-[10px] font-bold uppercase tracking-[0.2em] text-gray-400">Items</p><h2 className="mt-1 font-body-lg text-lg font-extrabold text-[#333333]">Produk Terjual</h2><p className="mt-1 font-body-sm text-xs text-gray-500">Estimasi UI: {formatCurrency(estimatedTotal)}. Total final tetap dihitung server.</p><FieldError message={form.errors.items} /></div><button className="rounded-full border border-[#1E4D3A] px-4 py-2 font-body-sm text-sm font-bold text-[#1E4D3A] transition hover:bg-[#1E4D3A] hover:text-white" onClick={addItem} type="button">Tambah Item</button></div><div className="mt-5 space-y-4">{form.data.items.map((item, index) => <div className="grid grid-cols-1 gap-3 rounded-2xl border border-[#E5E7EB] bg-[#F6F7F7] p-4 xl:grid-cols-[1fr_160px_auto]" key={item.row_id}><SelectField error={form.errors[`items.${index}.product_id`]} label="Product" name={`items.${index}.product_id`} onChange={(event) => updateItem(index, 'product_id', event.target.value)} value={item.product_id}><option value="">Pilih produk</option>{products.map((product) => <option key={product.id} value={product.id}>{product.name} - {formatCurrency(product.price)} / stok {product.stock_quantity}</option>)}</SelectField><TextField error={form.errors[`items.${index}.quantity`]} label="Quantity" name={`items.${index}.quantity`} onChange={(event) => updateItem(index, 'quantity', event.target.value)} type="number" value={item.quantity} /><div className="flex items-end"><button className="rounded-full border border-red-200 px-4 py-2 font-body-sm text-sm font-bold text-red-700 transition hover:bg-red-50 disabled:opacity-50" disabled={form.data.items.length === 1} onClick={() => removeItem(index)} type="button">Remove</button></div></div>)}</div></AdminCard><button className="rounded-full bg-[#1E4D3A] px-4 py-2 font-body-sm text-sm font-bold text-white transition hover:bg-[#013625] disabled:opacity-60" disabled={form.processing} type="submit">Simpan Offline Sale</button></form></div></>;
}

AdminOfflineSalesTambah.layout = (page) => <AdminLayout>{page}</AdminLayout>;

export default AdminOfflineSalesTambah;
